import React, { PropTypes } from 'react';
import './styles.scss';

const Autocomplete = (props) => 
    <span className='autocomplete-block'></span>


Autocomplete.propTypes = {
    children: PropType.any
}