let environments = {
    
}

environments['local'] = {
    serverUrl: 'https://localhost:8000'
}

environments['prod'] = {
    serverUrl: 'https://localhost:8000'
}

export default envs = environments;